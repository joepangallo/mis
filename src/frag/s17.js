/* ===== s17 ===== */
PROSE.s17 = `<span class="eyebrow">Application supplement &middot; 1&ndash;7</span>
<h2>The concepts as code</h2>

<p class="lede">Everything in this module so far has been described in words, because that is how the material is usually taught and how it will be examined. Words are also where these ideas go to become vague. This supplement writes four of them as functions instead, because a function cannot be vague &mdash; it either returns the right answer for a case you did not think of, or it does not.</p>

<div class="callout info"><b>How this supplement relates to the chapter.</b> Nothing here is new material. Each exercise is a definition you have already read, restated as a contract with test cases attached: the network effect, the difference between data and information, the categories of organizational information system, and Mason&rsquo;s questions about who may see what. The code is the check on whether the definition was actually understood, not an addition to it.</div>

<h3>Why a definition is worth writing as a function</h3>

<p>Prose lets you agree with a definition without ever committing to what it means at the edges. Code does not offer that comfort, and the edges are where the understanding lives:</p>

<ul class="keys">
<li><b>A function has to handle the empty case</b> &mdash; a report over no rows, a network with one member &mdash; and deciding what those return forces you to say what the concept actually claims when there is nothing to claim about.</li>
<li><b>A function has to resolve conflicts</b> &mdash; when a need looks like two categories at once, the order of your checks is your real definition, whatever the prose said.</li>
<li><b>A function fails visibly</b> &mdash; a misunderstanding that survives an essay shows up here as a red test with the value you actually returned printed next to the one you should have.</li>
</ul>

<p class="takeaway">If you can write it, you understood it. If you can only describe it, you may only have recognised it.</p>

<h3>How these run</h3>

<p>Each exercise gives you a stub, a stated contract and a set of tests. Write the body, run the tests, and read the failures &mdash; the value your code returned is printed beside the value expected, which is usually enough to see the mistake without a debugger.</p>

<ul class="split">
<li>Your code runs in this page, with no network and nothing installed.</li>
<li><b>Run the tests</b>, or press Ctrl-Enter while typing.</li>
<li>Tab indents rather than moving focus, so the editor behaves.</li>
<li>A loop that never ends is stopped after two seconds.</li>
<li><b>Reset the stub</b> puts the starting code back.</li>
<li><b>Show a solution</b> reveals one that passes, when you want it.</li>
</ul>

<p>There is more than one correct implementation of every one of these. The tests judge behaviour, not style, so a version that reads better than the one shown is a better answer, not a different one.</p>

<p class="mini">Every record in the test data below is <b>hypothetical</b> &mdash; invented so the functions have something to run against. No real customer, company or network is described.</p>
<div class="activity" data-activity="codeExercises"></div>

<h3>What the four have in common</h3>

<p>Read back over what you wrote and notice that none of the four functions needed a framework, a database or a network. Each is a small pure function over plain data, and each encodes a decision that a person would otherwise make inconsistently:</p>

<ol class="steps">
<li><b>connections</b> turns an intuition about popularity into arithmetic, and the arithmetic is what explains why platform markets tip toward a single winner rather than settling into a tie.</li>
<li><b>report</b> is the whole data-to-information distinction: a filter that says what counts and an accumulation that says what question is being answered.</li>
<li><b>classify</b> shows that a taxonomy is only as good as the order in which its tests are applied, which is why two people can agree on the definitions and still sort the same case differently.</li>
<li><b>redact</b> makes a privacy principle enforceable, and shows that the enforceable version is an allow-list, because a deny-list is a promise that decays every time the schema changes.</li>
</ol>

<div class="callout exam"><b class="tagline">The transferable idea</b>Business rules are code whether or not anyone writes them down. When they stay in prose they are applied differently by different people on different days; when they are written as a function with tests they are applied the same way every time and can be argued about precisely. A great deal of what an information system does for an organization is exactly that conversion.</div>

<h3>Where this shows up outside a course</h3>

<p>The four functions are small, but the shape is the one that recurs wherever software meets a business rule. Somebody states a policy in a sentence, somebody else has to make a machine apply it a million times, and the gap between those two is where most defects and most arguments live.</p>

<ul class="keys">
<li><b>The specification is rarely wrong; it is usually incomplete</b> &mdash; the sentence covers the cases everyone pictured, and the function has to answer the ones nobody did, which is why the empty and tied inputs above were the hardest part of each exercise.</li>
<li><b>The tests are where the disagreement surfaces</b> &mdash; two people can nod at the same definition and encode different behaviour, and a failing case is a much shorter conversation than two competing paragraphs.</li>
<li><b>The default matters more than the rule</b> &mdash; every one of these functions has a path taken when nothing matches, and what happens there decides how the system behaves on the day something unexpected arrives.</li>
</ul>

<p>None of that is specific to information systems. It is simply what it looks like when a policy stops being a description and starts being enforced, and this chapter&rsquo;s subject is an organization full of exactly that.</p>

<div class="activity" data-activity="codeQuiz"></div>`;

ACT.codeExercises = {"kind": "code", "label": "Write the code", "title": "Four definitions, four functions", "how": "Write each function so every test passes; a failing test prints what your code returned next to what was expected.", "objective": "1.7", "exercises": [{"prompt": "A network's value comes from the connections it makes possible, not from the count of members. Implement <b>connections(n)</b>: how many distinct two-way links exist among n members?", "signature": "connections(n) -> number", "starter": "function connections(n) {\n  // Every member can link to every other member,\n  // but a link between A and B is the same link as B and A.\n  return 0;\n}\n", "solution": "function connections(n) {\n  if (n < 2) return 0;\n  return (n * (n - 1)) / 2;\n}\n", "tests": [{"call": "connections(1)", "expect": 0, "note": "one member has nobody to link to"}, {"call": "connections(2)", "expect": 1}, {"call": "connections(5)", "expect": 10}, {"call": "connections(10)", "expect": 45, "note": "doubling the members did more than double the links"}, {"call": "connections(100)", "expect": 4950}, {"call": "connections(0)", "expect": 0, "note": "an empty network is still a valid input"}], "hint": "Each of the n members can reach n-1 others, which counts every link twice - once from each end.", "explain": "This is the network effect in one line. Members grow linearly and links grow with the square, which is why a platform that is slightly ahead on users can be far ahead on value, and why the second-place platform struggles to catch up."}, {"prompt": "Turn raw records into a management report. Implement <b>report(rows)</b>: given transaction rows, return an object mapping each region to its total revenue, including only completed sales.", "signature": "report(rows) -> { region: totalRevenue }", "starter": "function report(rows) {\n  // rows look like:\n  // { region: 'East', status: 'complete', units: 3, unitPrice: 20 }\n  // Skip anything not complete. Revenue is units * unitPrice.\n  return {};\n}\n", "solution": "function report(rows) {\n  return rows\n    .filter(function (r) { return r.status === 'complete'; })\n    .reduce(function (acc, r) {\n      acc[r.region] = (acc[r.region] || 0) + r.units * r.unitPrice;\n      return acc;\n    }, {});\n}\n", "tests": [{"call": "report([])", "expect": {}, "note": "no rows means no report, not an error"}, {"call": "report([{region:'East',status:'complete',units:2,unitPrice:10}])", "expect": {"East": 20}}, {"call": "report([{region:'East',status:'complete',units:2,unitPrice:10},{region:'East',status:'complete',units:1,unitPrice:5}])", "expect": {"East": 25}}, {"call": "report([{region:'East',status:'complete',units:2,unitPrice:10},{region:'West',status:'refunded',units:9,unitPrice:10}])", "expect": {"East": 20}, "note": "a refunded row must not reach the report"}, {"call": "report([{region:'North',status:'complete',units:1,unitPrice:100},{region:'South',status:'complete',units:2,unitPrice:50}])", "expect": {"North": 100, "South": 100}}], "hint": "Filter first, then accumulate. Watch the region you have not seen before - reading a missing key gives undefined, and undefined plus a number is not a number.", "explain": "You just wrote the difference between data and information. The rows are data: true, but unusable for a decision. The returned object is information, because it answers a question a manager actually asked. Nothing was added except structure and a rule about what counts."}, {"prompt": "Route a need to the right class of system. Implement <b>classify(need)</b> returning <code>'TPS'</code>, <code>'MIS'</code>, <code>'DSS'</code> or <code>'ESS'</code>.", "signature": "classify(need) -> 'TPS' | 'MIS' | 'DSS' | 'ESS'", "starter": "function classify(need) {\n  // need = { level, recordsEvent, comparesScenarios, scope }\n  //   recordsEvent      true when it must capture each event as it happens\n  //   comparesScenarios true when it must weigh what-if options\n  //   scope             'task' | 'function' | 'organization'\n  return 'TPS';\n}\n", "solution": "function classify(need) {\n  if (need.recordsEvent) return 'TPS';\n  if (need.comparesScenarios) return 'DSS';\n  if (need.scope === 'organization') return 'ESS';\n  return 'MIS';\n}\n", "tests": [{"call": "classify({level:'operational',recordsEvent:true,comparesScenarios:false,scope:'task'})", "expect": "TPS", "note": "capturing each sale as it happens"}, {"call": "classify({level:'managerial',recordsEvent:false,comparesScenarios:false,scope:'function'})", "expect": "MIS", "note": "a routine summary of one function"}, {"call": "classify({level:'managerial',recordsEvent:false,comparesScenarios:true,scope:'function'})", "expect": "DSS", "note": "weighing options, not reporting them"}, {"call": "classify({level:'executive',recordsEvent:false,comparesScenarios:false,scope:'organization'})", "expect": "ESS"}, {"call": "classify({level:'executive',recordsEvent:true,comparesScenarios:true,scope:'organization'})", "expect": "TPS", "note": "if an event must be recorded, that comes first whoever asked"}], "hint": "Order matters. Test the most specific condition first, and let the general summary be what falls through at the end.", "explain": "The last test is the interesting one. A system that must record events is a transaction processing system no matter how senior the person asking, because everything else is built on the record it keeps. Getting the order of the checks right is the same as getting the reasoning right."}, {"prompt": "Mason's <b>property</b> and <b>privacy</b> questions become a function. Implement <b>redact(record, purpose)</b>: return a copy holding only the fields the stated purpose actually needs.", "signature": "redact(record, purpose) -> object", "starter": "function redact(record, purpose) {\n  // purpose 'shipping'  needs name and address\n  // purpose 'analytics' needs region and totalSpend\n  // purpose 'support'   needs name and orderId\n  // Anything else: return an empty object rather than guessing.\n  return record;\n}\n", "solution": "function redact(record, purpose) {\n  var allowed = {\n    shipping: ['name', 'address'],\n    analytics: ['region', 'totalSpend'],\n    support: ['name', 'orderId']\n  }[purpose] || [];\n  var out = {};\n  allowed.forEach(function (f) {\n    if (f in record) out[f] = record[f];\n  });\n  return out;\n}\n", "tests": [{"call": "redact({name:'A',address:'1 High St',ssn:'000',region:'East',totalSpend:50,orderId:'X1'},'shipping')", "expect": {"name": "A", "address": "1 High St"}}, {"call": "redact({name:'A',address:'1 High St',ssn:'000',region:'East',totalSpend:50,orderId:'X1'},'analytics')", "expect": {"region": "East", "totalSpend": 50}, "note": "analytics never needs to know who"}, {"call": "redact({name:'A',address:'1 High St',ssn:'000',region:'East',totalSpend:50,orderId:'X1'},'support')", "expect": {"name": "A", "orderId": "X1"}}, {"call": "redact({name:'A',ssn:'000'},'marketing')", "expect": {}, "note": "an unrecognised purpose gets nothing"}, {"call": "redact({region:'West'},'shipping')", "expect": {}, "note": "asking for a field the record does not have must not invent one"}], "hint": "Build the result from the allow-list rather than deleting from the record. A deny-list quietly leaks every field somebody adds later.", "explain": "The allow-list is the whole point. Written as a deny-list, this function is correct today and wrong the moment a new column lands in the table, because nobody remembers to add it. That is how most real privacy failures happen - not by decision, but by default."}]};

ACT.codeQuiz = {"kind": "quiz", "label": "Check yourself", "title": "What the code made explicit", "how": "These ask about the reasoning the exercises forced, not about JavaScript syntax.", "objective": "1.7", "questions": [{"q": "Your <code>classify</code> function checks <code>recordsEvent</code> before it checks anything else, so a need that both records events and compares scenarios comes back as <code>'TPS'</code>. What does that ordering actually assert?", "opts": ["That executives matter less than operational staff when systems are prioritised", "That a system which must capture events is first of all a record of them, because everything reported or modelled later is built on that record", "That transaction processing systems are more common in organizations than any other category", "That comparing scenarios is not a real requirement unless it is the only requirement stated"], "a": 1, "why": ["Priority between people is not what the branch expresses. The same ordering applies whoever raised the need, which is precisely why it is a definition rather than a preference.", "Right. The order of the checks is the claim: if events must be captured, that obligation is foundational, because a summary or a model can only ever be as good as the record it reads from.", "Frequency is not what the function measures, and it never sees a population of systems. Even if transaction systems were rare the ordering would be unchanged.", "The scenario requirement is real and is handled on the very next line. It is subordinate here, not dismissed, which is a different claim entirely."]}, {"q": "In <code>report</code>, the filter for completed rows runs before the totals are accumulated. A colleague suggests totalling everything first and subtracting refunds afterwards. What is the risk?", "opts": ["Nothing, because subtracting the refunds afterwards reaches the same total by a different route", "The totals would be slower to compute on a large number of rows", "Any row whose status is neither complete nor refunded is silently included in the total, because subtracting handles only the case you remembered", "Refunded rows would be counted twice rather than removed"], "a": 2, "why": ["The two routes agree only while completed and refunded are the only statuses that exist. As soon as a pending or disputed row appears, the filter leaves it out and the subtraction quietly keeps it in.", "Performance is not the objection, and on realistic volumes the difference would not be noticeable. The concern is which rows end up in the number.", "Right. Filtering states what belongs in the answer; subtracting states what to remove from it. The second only handles cases you anticipated, so a new status defaults into the total instead of out of it.", "Double counting is not what happens; each row is read once either way. The problem is the rows that are neither completed nor refunded."]}, {"q": "<code>connections(100)</code> returns 4,950 while <code>connections(10)</code> returns 45. A competitor has 60 members to your 100. What does the arithmetic support saying?", "opts": ["That your network is worth about 1.7 times theirs, in proportion to the members", "That your network offers roughly 2.8 times as many possible connections, so a lead in members is a larger lead in what the network can do", "That their network cannot grow, because yours has already taken the available members", "That connections are the only thing that determines what a network is worth"], "a": 1, "why": ["That is the linear reading, and it is the one the arithmetic contradicts: value here is not proportional to members, which is the entire reason the function is worth writing.", "Right. 4,950 against 1,770 is close to 2.8 times, so a 1.7-fold lead in members is a 2.8-fold lead in possible connections. That gap is what makes catching up hard.", "Nothing in the calculation says membership is fixed or exclusive, and both networks can grow at once. The function counts links, not market share.", "The function counts possible connections, which is one input to value rather than the whole of it. Whether those connections are used at all is a separate question."]}, {"q": "<code>redact</code> builds its result from an allow-list. Six months later a <code>dateOfBirth</code> column is added to the customer table. What happens to the analytics output?", "opts": ["It gains the new field, because analytics receives whatever the record contains", "It is unchanged, because a field has to be named before it can be returned", "It throws an error, because the function encounters a field it does not recognise", "It gains the field only if the record was created after the column was added"], "a": 1, "why": ["That is the deny-list behaviour, and it is exactly the failure the allow-list prevents. Under a deny-list the new column would flow through untouched.", "Right. Nothing is returned unless it appears in the allow-list, so a new column is excluded by default and stays excluded until somebody decides otherwise. Safe by default is the whole design.", "The function never inspects fields it was not asked for, so an unfamiliar one cannot raise an error. It is simply not read.", "When the record was created makes no difference; the function reads the same allow-list every time it runs, regardless of the record's age."]}]};

